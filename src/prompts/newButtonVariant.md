There is in figma a new button variant called "Redsift" that has a version for both Medium and Small size. I will need you to update the code to incorporate this new variant. For that purpose you need to:

1. Update types with the new variant.
2. Update the css with the proper styles defined at figma for that new variant.
3. Update code connect configuration (\*.figma.tsx file).
4. Update the storybook file to be able to access to the new code added.

Use the figma MCP avaliable getting the figma needed information

MODEL: GPT-5,1-Codex
